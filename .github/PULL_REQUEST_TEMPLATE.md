## What this changes

<!-- What does this do, and why is it needed? Link an issue if there is one. -->

## How it was verified

<!-- Which tests cover it? If it changes reconcile behaviour, which test fails
     without this change? -->

- [ ] `npm run verify` passes
- [ ] New or changed behaviour has a test that fails without this change
- [ ] CRD, RBAC and examples updated if the API changed
- [ ] `docs/` and the README updated if user-facing behaviour changed

## Risk

<!-- What could this break? Does it change anything a running operator would do
     to existing Hetzner resources on its next reconcile? Anything that could
     delete or reboot infrastructure deserves a sentence here. -->
